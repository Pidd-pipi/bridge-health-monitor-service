const rows=document.querySelector('#rows'),message=document.querySelector('#message');async function load(){const r=await fetch('/api/v1/bridges');const d=await r.json();rows.innerHTML=d.items.map(x=>`<tr><td>${x.id}</td><td>${x.name}</td><td>${x.river}</td><td>${x.condition}</td><td>${x.risk_score}</td><td><button data-id="${x.id}">设为关注</button></td></tr>`).join('');rows.querySelectorAll('button').forEach(b=>b.onclick=()=>update(b.dataset.id))}async function update(id){const r=await fetch(`/api/v1/bridges/${id}/condition`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({condition:'watch'})});message.textContent=r.ok?'状态已更新':'更新失败';if(r.ok)load()}document.querySelector('#refresh').onclick=load;load().catch(()=>message.textContent='无法读取桥梁记录');
